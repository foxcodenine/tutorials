bun add @tiptap/react @tiptap/pm @tiptap/starter-kit

bun add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths

bun create playwright

Add unit and e2e testing to this Next.js app. Use Vitest for unit testing and Playwright for e2e testing. The libraries are already installed.

For properly configuring the project, see these docs for Next.js + Vitest:
<nextjs-vitest-setup-docs>

</nextjs-vitest-setup-docs>

<nextjs-[laywright-setup-doc]>

</nextjs-[laywright-setup-doc]>

Also use the context7 mcp for looking up more docs (eg Vitest, Playwright) as needed.
bun add @tiptap/react @tiptap/pm @tiptap/starter-kit

bun add -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths

bun create playwright

Add unit and e2e testing to this Next.js app. Use Vitest for unit testing and Playwright for e2e testing. The libraries are already installed.

For properly configuring the project, see these docs for Next.js + Vitest:
<nextjs-vitest-setup-docs>
[Pasted Content 5626 chars]
</nextjs-vitest-setup-docs>

<nextjs-[laywright-setup-doc]>
[Pasted Content 4844 chars]
</nextjs-[laywright-setup-doc]>

Also use the context7 mcp for looking up more docs (eg Vitest, Playwright) as needed.

Add both unit and e2e tests for the existing app. Add a comprehensive test suite that tests all cases, including potential errors or things that could go wrong. Write tests that test the intended behavior, NOT tests that just pass => The code should work as intended, tests should fail if it doesn't.
