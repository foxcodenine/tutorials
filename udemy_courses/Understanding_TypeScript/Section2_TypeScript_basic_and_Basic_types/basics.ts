let userName: string;
userName = 'Max';

// ---------------------------------------------------------------------

let userAge = 40;

let something: any = 'Hello World'
something = 5;

// ---------------------------------------------------------------------

function add(a: number, b = 5): number {
    return a + b
}

add(5);

// ---------------------------------------------------------------------
